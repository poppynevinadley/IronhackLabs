#1. Write a query to display for each store its store ID, city, and country.
SELECT * from city;     #country = country_id | city = city, country_id, 
select store.store_id, city.country_id, city_id
	from staff 
	join address 
	USING (address_id);